/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { TrackPipe } from './track.pipe';

describe('TrackPipe', () => {
  it('create an instance', () => {
    const pipe = new TrackPipe();
    expect(pipe).toBeTruthy();
  });
});
